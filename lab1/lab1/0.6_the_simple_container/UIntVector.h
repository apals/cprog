#ifndef UINTVECTOR
#define UINTVECTOR

class UIntVector {

    private:
        size_t size;
    public:
        UIntVector();
        UIntVector(size_t);
        UIntVector(initializer_list);
}

#endif
